services:
  face:
    build: ./frontend
    container_name: ai-project_face_1
    ports:
      - "3000:3000"  # <--- THIS LINE IS MISSING OR INCORRECT
    restart: always

  brain:
    build: ./backend
    container_name: ai-project_brain_1
    ports:
      - "8000:8000"
    restart: always